#include <stdio.h>
#include <stdlib.h>

#include "fila.h"

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	
	
// contadores;
int i=0;
	imprime();
	
	do {
		printf("Bem vindo ao escalonador de processos");
		printf("Digite os processos ");
		printf("em formato: <id, tempo de chegada, tempo de execução e tipo>");
	}
	
	return 0;
}
